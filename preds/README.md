# Prediction Outputs

Saved model prediction CSVs are stored in `preds/preds/`.

These files correspond to:

- Elastic Net experiments
- XGBoost experiments
- ARIMA baseline outputs
- TCN multi-seed runs
- TCN ablation runs

